<div class="d-flex row">
  <div class="card col">
    <div class="card-body py-5">
      <h5 class="mb-3"><%= workout.workout_date.strftime("%a") %></h5>
      <h6 class="mb-3"><%= workout.workout_date.strftime("%d %b") %></h6>
      <%= link_to 'Edit', edit_workout_path(workout) %>
    </div>
  </div>
  <%= render partial: 'lift_workout', collection: workout.lift_workouts %>
</div>
